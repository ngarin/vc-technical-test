import { DateSincePipe } from './date-since.pipe';

describe('DateSincePipe', () => {
  it('create an instance', () => {
    const pipe = new DateSincePipe();
    expect(pipe).toBeTruthy();
  });
});
